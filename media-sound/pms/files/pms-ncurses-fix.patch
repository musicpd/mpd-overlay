Index: src/mycurses.h
===================================================================
--- src/mycurses.h	(revision 391)
+++ src/mycurses.h	(working copy)
@@ -22,11 +22,7 @@
  */
 
 
-#ifdef HAVE_NCURSESW_H
-	#include <ncursesw.h>
-#elif HAVE_NCURSES_H
-	#include <ncurses.h>
-#elif HAVE_CURSES_H
+#if HAVE_CURSES_H
 	#include <curses.h>
 #else
 	#include <ncurses.h>
